/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,  // ← Add this line (or change to true if exists)
};

export default nextConfig;